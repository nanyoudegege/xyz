# 实地解剖学 OCR 精校进度 / Field Anatomy OCR QC

这是一个**公开的质量控制与进度管理仓库**，用于跟踪《实地解剖学》第2版扫描PDF的逐页视觉核对、隐藏OCR文字层重建和回归验证。

> 版权边界：仓库不分发教材扫描件、完整OCR文本、逐页图片或校定PDF。这里只保存通用脚本、质量标准和不含教材正文的进度/审计元数据。

## 当前检查点

- 最新验证版本：**v170**
- 本轮批次：PDF **64-113** 页 / 书内 **41-90** 页，共 **50页**
- 严格逐页、从原图重建：目录 PDF 6-23 页；正文书内 **1-40页**
- 批量坐标深校与高风险视觉订正：正文已连续覆盖至书内 **90页**
- 本轮字符：**40,339个**（非空 **39,001个**）
- 原扫描明确支持的文字订正：**33处**
- 高风险区域人工重建：**27组、632个非空字符，涉及12页**
- 非空字符框零墨迹：**0**
- 全书低分辨率可见回归：**592/592页一致**
- 本轮全部50页PDFium 96 dpi可见回归：**50/50一致**
- 代表性15页PDFium与Poppler 180 dpi双渲染回归：与基线及原扫描均一致
- 书签：706项；批注：43页、282项；页码标签保留
- 下一批：PDF **114-163** 页 / 书内 **91-140** 页，共 **50页**

## 两种完成状态

- `complete_sequential_rebuild`：逐页以原扫描为准，从头重建整页文字层。
- `complete_batch_deepcheck`：以既有文字层为基线，对整批字符坐标重建，并对高风险正文、图注、标签和表格区域进行原图视觉订正；仍保留后续全书语义终检。

因此，v170已经完成50页批量深校和坐标验证，但不把它表述为“50页全部从零人工重打、绝对零错”。

## 进度文件

- [`progress/progress.csv`](progress/progress.csv)：逐版本检查点
- [`progress/page_status.csv`](progress/page_status.csv)：覆盖592页的分段状态
- [`progress/special_remediation.csv`](progress/special_remediation.csv)：早期高风险页定点修复记录
- [`progress/checkpoints/v170.json`](progress/checkpoints/v170.json)：本轮50页检查点元数据
- [`docs/workflow.md`](docs/workflow.md)：精校与验证流程
- [`docs/audit-schema.md`](docs/audit-schema.md)：审计字段说明

## 验收口径

每一批至少完成以下检查：

1. 原扫描图像视觉检查，优先复核低置信度正文、图题、标签和表格；
2. 替换目标页旧OCR流，按真实印刷位置重新建立字符坐标；
3. 内容流字符顺序、操作数量和坐标可回读；
4. 每个非空字符框与原扫描墨迹相交；
5. 编辑页可见渲染不变，并对代表性高风险页进行双渲染器高分辨率回归；
6. 未编辑页、书签、页码标签、批注和页面几何不受影响；
7. 批量模式完成后，仍安排一次全书语义终检。

## 处理节奏

默认按 **50页一个批次** 推进并集中交付，只有出现文件结构损坏、页序异常或无法可靠裁决的系统性问题时才提前停止。

## 仓库名称

当前使用此前空置的公开仓库 `nanyoudegege/xyz` 承载项目。建议后续在GitHub设置中将其重命名为 `field-anatomy-ocr-qc`；重命名不会改变仓库历史。
